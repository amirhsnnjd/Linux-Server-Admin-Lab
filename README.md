# Linux-Server-Admin-Lab

این راهنما تمام دستورات ضروری لینوکس را که در دوره lpic-1 دیدم ساپورت میکنه


## 🧩 1) First commnads

| دستور (Command) | توضیح فارسی | example |
| :--- | :--- | :--- |
| `type command` |نشون میده دستورات از چه نوعی هستند  | `type echo   -> echo is a shell builtin` |
| `history` | سابقه ی کامند هایی که تا حالا زدیم رو نشون میده| |
| `echo $HISTSIZE` |تعداد کامندی که تو هیستوری ذخیره میکنه| `echo $HISTSIZE -> 1000` |
| `pwd` |مکان فعلی که توش هستیم رو بهمون برمیگردونه| `pwd -> /home/server` |
| `env` |متغیر های سیستم رو نشون میده| |
| `export` |متغیر های سیستم رو نشون میده| |
| `echo $PATH` |جاهایی که سیستم توش دنبال مسیر میگرده| |
| `export ENV` |متغیری که ست میکنی تو ساب شل ها هم در دسترس قرار میگیره| `export name="Ali"`|
| `unset ENV` |پاک کردن یک متغیر از سیستم| `unset name`|
| `man -k command` |پیدا کردن توضیح در دستورات که نمیدونیم دقیق کامندش چیه| `man -k passwd`|
| `!!` |اجرا کردن دستور قبلی ||



## 🧩 2) nano shortcuts

| دستور (Command)                     | توضیح فارسی                                    |
| :---------------------------------- | :--------------------------------------------- |
| `Ctrl+S`                            | ذخیره فایل فعلی                                |
| `Ctrl+O`                            | ذخیره فایل با نام یا مسیر جدید، مثل Save as    |
| `Ctrl+R`                            | وارد کردن یک فایل داخل فایل فعلی               |
| `Ctrl+X`                            | بستن بافر و خروج از nano                       |
| `Ctrl+K`                            | بریدن خط فعلی و انتقال به cutbuffer            |
| `Alt+6`                             | کپی کردن خط فعلی داخل cutbuffer                |
| `Ctrl+U`                            | چسباندن محتوای cutbuffer                       |
| `Ctrl+]`                            | کامل کردن کلمه فعلی                            |
| `Alt+3`                             | کامنت یا آن‌کامنت کردن خط یا محدوده انتخاب‌شده |
| `Alt+U`                             | برگرداندن آخرین تغییر، Undo                    |
| `Alt+E`                             | انجام دوباره آخرین تغییر برگشت‌خورده، Redo     |
| `Ctrl+B`                            | شروع جستجو به سمت عقب                          |
| `Ctrl+F`                            | شروع جستجو به سمت جلو                          |
| `Alt+B`                             | پیدا کردن مورد بعدی به سمت عقب                 |
| `Alt+F`                             | پیدا کردن مورد بعدی به سمت جلو                 |
| `Alt+R`                             | شروع عملیات جایگزینی متن                       |
| `Ctrl+H`                            | حذف کاراکتر قبل از کرسر                        |
| `Ctrl+D`                            | حذف کاراکتر زیر کرسر                           |
| `Alt+Bsp`                           | حذف کلمه سمت چپ                                |
| `Ctrl+Del`                          | حذف کلمه سمت راست                              |
| `Alt+Del`                           | حذف خط فعلی                                    |
| `Ctrl+T`                            | اجرای یک دستور                                 |
| `Ctrl+T Ctrl+S`                     | اجرای بررسی املایی                             |
| `Ctrl+T Ctrl+Y`                     | اجرای بررسی سینتکس                             |
| `Ctrl+T Ctrl+O`                     | اجرای فرمت‌کننده متن                           |
| `Tab`                               | تورفتگی دادن به محدوده انتخاب‌شده              |
| `Shift+Tab`                         | حذف تورفتگی از محدوده انتخاب‌شده               |
| `Ctrl+J`                            | مرتب‌سازی یا justify کردن پاراگراف یا محدوده   |
| `Alt+J`                             | مرتب‌سازی یا justify کردن کل بافر              |
| `Alt+T`                             | بریدن متن تا انتهای بافر                       |
| `Alt+V`                             | وارد کردن کلید بعدی به صورت خام و بدون پردازش  |
| `Alt+Comment/uncomment line/region` | کامنت یا آن‌کامنت کردن خط یا محدوده            |
| `Alt+U`                             | Undo کردن آخرین عملیات                         |
| `Alt+E`                             | Redo کردن آخرین عملیات Undo شده                |
| `←`                                 | حرکت یک کاراکتر به عقب                         |
| `→`                                 | حرکت یک کاراکتر به جلو                         |
| `Ctrl+←`                            | حرکت یک کلمه به عقب                            |
| `Ctrl+→`                            | حرکت یک کلمه به جلو                            |
| `Ctrl+A`                            | رفتن به ابتدای خط                              |
| `Ctrl+E`                            | رفتن به انتهای خط                              |
| `Ctrl+P`                            | رفتن یک خط به بالا                             |
| `Ctrl+N`                            | رفتن یک خط به پایین                            |
| `Ctrl+↑`                            | رفتن به بلاک قبلی                              |
| `Ctrl+↓`                            | رفتن به بلاک بعدی                              |
| `Alt+Home`                          | رفتن به اولین ردیف قابل مشاهده در صفحه         |
| `Alt+End`                           | رفتن به آخرین ردیف قابل مشاهده در صفحه         |
| `Ctrl+Y`                            | رفتن یک صفحه به بالا                           |
| `Ctrl+V`                            | رفتن یک صفحه به پایین                          |
| `Alt+\`                             | رفتن به ابتدای بافر                            |
| `Alt+/`                             | رفتن به انتهای بافر                            |
| `Alt+G`                             | رفتن به یک خط مشخص                             |
| `Alt+]`                             | رفتن به براکت متناظر                           |
| `Alt+<`                             | اسکرول نمای صفحه به سمت چپ                     |
| `Alt+>`                             | اسکرول نمای صفحه به سمت راست                   |
| `Alt+↑`                             | اسکرول نمای صفحه به بالا                       |
| `Alt+↓`                             | اسکرول نمای صفحه به پایین                      |
| `Ctrl+L`                            | قرار دادن خط فعلی در مرکز صفحه                 |
| `Alt+←`                             | رفتن به بافر قبلی                              |
| `Alt+→`                             | رفتن به بافر بعدی                              |
| `Ctrl+C`                            | نمایش موقعیت کرسر                              |
| `Alt+D`                             | نمایش تعداد خط، کلمه و کاراکترها               |
| `Ctrl+G`                            | نمایش متن راهنما                               |
| `Alt+A`                             | فعال یا غیرفعال کردن انتخاب/Mark               |
| `Alt+C`                             | روشن یا خاموش کردن نمایش دائمی موقعیت          |
| `Alt+N`                             | روشن یا خاموش کردن شماره خطوط                  |
| `Alt+P`                             | روشن یا خاموش کردن نمایش فاصله‌های سفید        |
| `Alt+S`                             | روشن یا خاموش کردن soft wrapping               |
| `Alt+X`                             | مخفی یا نمایان کردن خطوط راهنما                |
| `Alt+Z`                             | مخفی یا نمایان کردن نوارهای اطلاعات            |


## 🧩 3) file proccessing

| دستور (Command) | توضیح فارسی | example |
| :--- | :--- | :--- |
| `split -l 10 filename prefixname` | فایل را به چند فایل کوچک‌تر تقسیم می‌کند؛ هر فایل خروجی شامل ۱۰ خط خواهد بود. نام فایل‌های خروجی با `prefixname` شروع می‌شود. | `split -l 10 users.txt part_` → `part_aa`, `part_ab` |
| `split -l 10 filename prefixname -d` | فایل را به بخش‌های ۱۰ خطی تقسیم می‌کند و برای نام فایل‌های خروجی به‌جای حروف از اعداد استفاده می‌کند. | `split -l 10 users.txt part_ -d` → `part_00`, `part_01` |
| `sort filename` | خطوط داخل فایل را به‌صورت الفبایی مرتب می‌کند. | `sort names.txt` |
| `sort -n filename` | خطوط فایل را بر اساس مقدار عددی، از کوچک به بزرگ مرتب می‌کند. | `sort -n numbers.txt` |
| `sort -nr filename` | خطوط فایل را بر اساس مقدار عددی، از بزرگ به کوچک مرتب می‌کند. | `sort -nr numbers.txt` |
| `less filename` | محتوای فایل را به‌صورت صفحه‌به‌صفحه نمایش می‌دهد و امکان جستجو و جابه‌جایی داخل فایل را فراهم می‌کند. | `less access.log` |
| `head -n 10 filename` | ۱۰ خط اول فایل را نمایش می‌دهد. | `head -n 10 users.txt` |
| `tail -n 10 filename` | ۱۰ خط آخر فایل را نمایش می‌دهد. | `tail -n 10 access.log` |
| `tail -f filename` | تغییرات جدید فایل را به‌صورت زنده نمایش می‌دهد؛ معمولاً برای بررسی فایل‌های لاگ استفاده می‌شود. | `tail -f /var/log/nginx/access.log` |
| `wc -l filename` | تعداد خطوط موجود در فایل را نمایش می‌دهد. | `wc -l users.txt` → `120 users.txt` |
| `uniq filename` | خطوط تکراری پشت‌سرهم را حذف می‌کند. بهتر است قبل از آن از `sort` استفاده شود. | `sort names.txt \| uniq` |
| `md5sum filename` | هش MD5 فایل را محاسبه می‌کند؛ معمولاً برای بررسی تغییر نکردن یا سالم بودن فایل استفاده می‌شود. | `md5sum backup.zip` |
| `sha256sum filename` | هش SHA-256 فایل را محاسبه می‌کند و نسبت به MD5 امنیت و دقت بیشتری دارد. | `sha256sum backup.zip` |



## 🧩 4) grep & regular expression

| دستور (Command) | توضیح فارسی | example |
| :--- | :--- | :--- |
| `grep "hi" filename` | خط‌هایی از فایل را نمایش می‌دهد که عبارت `hi` داخل آن‌ها وجود دارد. | `grep "error" log.txt` |
| `grep -v "hi" filename` | خط‌هایی را نمایش می‌دهد که عبارت `hi` داخل آن‌ها وجود ندارد. | `grep -v "success" log.txt` |
| `egrep "^hi" filename` | خط‌هایی را نمایش می‌دهد که با عبارت `hi` شروع می‌شوند. علامت `^` به معنی ابتدای خط است. | `egrep "^root" users.txt` |
| `grep -i "hi" filename` | عبارت را بدون حساسیت به حروف بزرگ و کوچک جستجو می‌کند. | `grep -i "linux" file.txt` |
| `grep -c "hi" filename` | تعداد خط‌هایی را نمایش می‌دهد که عبارت `hi` داخل آن‌ها وجود دارد. | `grep -c "error" log.txt` |
| `egrep -R "^hi" *` | داخل تمام فایل‌ها و پوشه‌های مسیر فعلی به‌صورت بازگشتی، خط‌هایی را پیدا می‌کند که با `hi` شروع می‌شوند. | `egrep -R "^Error" *` |
| `egrep -d skip "^hi" *` | عبارت را داخل فایل‌های مسیر فعلی جستجو می‌کند، اما پوشه‌ها را نادیده می‌گیرد. | `egrep -d skip "^root" *` |
| `echo hi > test` | عبارت `hi` را داخل فایل `test` می‌نویسد. اگر فایل محتوا داشته باشد، محتوای قبلی پاک می‌شود. | `echo "Hello" > test.txt` |
| `echo hi >> test` | عبارت `hi` را به انتهای فایل `test` اضافه می‌کند، بدون اینکه محتوای قبلی پاک شود. | `echo "New line" >> test.txt` |
| `ls ali 2> test` | خطاهای دستور را داخل فایل `test` ذخیره می‌کند. عدد `2` نشان‌دهنده خروجی خطا یا `stderr` است. | `ls notfound 2> errors.txt` |
| `ls ali &> test` | هم خروجی عادی و هم خطاهای دستور را داخل فایل `test` ذخیره می‌کند. | `ls /root &> output.txt` |
| `cat < file1 > file2` | محتوای `file1` را به‌عنوان ورودی دریافت کرده و داخل `file2` می‌نویسد. محتوای قبلی `file2` پاک می‌شود. | `cat < source.txt > destination.txt` |
| `> filename` | محتوای فایل را به‌طور کامل پاک می‌کند، ولی خود فایل باقی می‌ماند. اگر فایل وجود نداشته باشد، آن را ایجاد می‌کند. | `> access.log` |
| `echo "Linux behtarin ose, Linux linux linx" \| sed 's/Linux/BSD/'` | فقط اولین عبارت `Linux` در هر خط را با `BSD` جایگزین می‌کند. | خروجی: `BSD behtarin ose, Linux linux linx` |
| `echo "Linux behtarin ose, Linux linux linx" \| sed 's/Linux/BSD/g'` | تمام عبارت‌های دقیق `Linux` را در خط با `BSD` جایگزین می‌کند. به حروف بزرگ و کوچک حساس است. | خروجی: `BSD behtarin ose, BSD linux linx` |
| `echo "Linux behtarin ose, Linux linux linx" \| sed 's/Linux/BSD/gi'` | تمام حالت‌های عبارت `Linux` را بدون حساسیت به حروف بزرگ و کوچک با `BSD` جایگزین می‌کند. | خروجی: `BSD behtarin ose, BSD BSD linx` |